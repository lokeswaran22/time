# Server Configuration
PORT=3000

# MySQL Configuration (only needed if using MySQL version)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=timesheet_db

# Microsoft Teams / Azure AD Configuration
# Get these from Azure Portal: https://portal.azure.com
# 1. Register an app in Azure AD
# 2. Copy the Application (client) ID
# 3. Copy the Directory (tenant) ID
# 4. Generate a client secret
MSAL_CLIENT_ID=your_client_id_here
MSAL_TENANT_ID=your_tenant_id_here
MSAL_CLIENT_SECRET=your_client_secret_here
MSAL_REDIRECT_URI=http://localhost:3000
JWT_SECRET=your_jwt_secret_here_use_random_string

# Note: For SQLite version (default), no database configuration needed
# The SQLite database file (timesheet.db) will be created automatically
